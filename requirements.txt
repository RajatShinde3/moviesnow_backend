# ───────── Core web stack
fastapi
uvicorn[standard]

# ───────── Database (async + alembic)
SQLAlchemy
asyncpg
psycopg2-binary
alembic

# ───────── Settings & serialization
pydantic
pydantic-settings
python-dotenv
orjson

# ───────── Auth / security
passlib
bcrypt
python-jose[cryptography]
pyotp
email-validator
python-multipart

# ───────── Caching / rate limiting (used by APIs)
redis
slowapi

# ───────── Utilities your code references
python-slugify

# ───────── Test deps (your suite uses async + factories)
pytest
pytest-asyncio
pytest-dotenv
pytest-mock
freezegun
Faker
httpx
